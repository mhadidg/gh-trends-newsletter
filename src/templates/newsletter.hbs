GitHub Trends — Week
{{year}}-W{{week}}
═════════════════════════════ 🚀 Top
{{repositories.length}}
trending repositories this week:

{{#each repositories}}
  {{add @index 1}}.
  {{nameWithOwner}}
  ⭐
  {{formatNumber stargazerCount}}
  {{#if description}}{{description}}{{else}}No description available{{/if}}
  {{url}}

{{/each}}
--- Generated by GitHub Trends Newsletter